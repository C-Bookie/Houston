
#include "buffer.h"
#include <Arduino.h>

//Buffer* newBuffer() {
//  Buffer* buffer = (Buffer*)malloc(sizeof(Buffer));
//  *buffer = Buffer {
//    .packet = NULL,
//    #if USING_HEADER
//      .header = (unsigned char*)malloc(HEADER_LEN),
//    #endif
//    // .len = 0
//  };
//  return buffer;
//}
